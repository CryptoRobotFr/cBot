# cBot

/!\ This code may not work properly if you don't have the same version of package especially the ccxt package /!\
To install a working version of package run the command below :
```
pip install -r requirements.txt
```
